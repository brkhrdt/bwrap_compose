# ls tool manifest for bwrap_compose
# Provides /bin/ls inside a minimal sandbox.

mounts:
  - host: /bin/ls
    container: /bin/ls
    mode: ro
  - host: /usr/lib/libc.so.6
    container: /usr/lib/libc.so.6
    mode: ro
  - host: /usr/lib/libcap.so.2
    container: /usr/lib/libcap.so.2
    mode: ro
  - host: /lib64/ld-linux-x86-64.so.2
    container: /lib64/ld-linux-x86-64.so.2
    mode: ro

tmpfs: /

args:
  - "--unshare-all"
  - "--die-with-parent"
  - "--new-session"
  - "--dir"
  - "/bin"
  - "--dir"
  - "/lib64"
  - "--dir"
  - "/usr/lib"
  - "--dir"
  - "/usr/lib/x86_64-linux-gnu"

run:
  - /bin/ls
